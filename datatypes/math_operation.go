package datatypes

import "fmt"

func MathOperations() {
	var result = 999 + 1

	var addition = 60 + 9
	var subtract = 100 - 1
	var multiply = 20 * 20
	var division = 1945 / 78

	fmt.Println(result, subtract, addition, multiply, division)
}
